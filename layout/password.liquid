<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ shop.name }}</title>
    <link rel="stylesheet" href="{{ 'wango-base.css' | asset_url }}">
    <link rel="stylesheet" href="{{ 'wango-sections.css' | asset_url }}">
    {{ content_for_header }}
  </head>
  <body>
    <main class="w-page">
      <div class="w-page__inner" style="text-align:center">
        <div class="w-wordmark" style="font-size:2rem">wang<i>o</i></div>
        {{ content_for_layout }}
      </div>
    </main>
  </body>
</html>
